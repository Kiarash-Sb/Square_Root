#include<iostream>
#include<math.h>
using namespace std;
int main()
{
	float num, root;
	cout << "Enter a number:	";
	cin >> num;
	root = sqrt(num);
	cout << "Square root of " << num << " is " << root;
	return 0;
}
